---
title: Layout
navigation:
  - text: 'Spacing'
    to: '/guides/layout/spacing'
  - text: 'Grid'
    to: '/guides/layout/grid'
  - text: 'Code'
    to: '/guides/layout/code'
---

import { SPACING, Text, COLORS } from '@umich-lib/core'
import Table from '../../../components/table'

<Text lede>Consistent spacing creates visual balance that makes the UI easier to scan. U-M Library’s Design System spacing scale can be applied to both margin or padding properties for both small, refined spacing situations, as well as to large spatial differences.</Text>

<Table>
  <thead>
    <tr>
      <th>Size</th>
      <th>rem</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    {Object.keys(SPACING).map(size => (
      <tr>
        <td>{size}</td>
        <td>{SPACING[size]}</td>
        <td><div style={{
          display: 'inline-block',
          height: SPACING[size],
          width: SPACING[size],
          background: COLORS.teal[400],
        }}></div></td>
      </tr>
    ))}
  </tbody>
</Table>
